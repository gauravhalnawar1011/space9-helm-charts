apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater

metadata:
  name: platform-image-updater
  namespace: argocd

spec:
  applicationRefs:
    - namePattern: "backend-dev"

      labelSelectors:
        matchLabels:
          image-updater: platform

      useAnnotations: true